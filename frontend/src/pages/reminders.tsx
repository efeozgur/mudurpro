import { useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { Plus, Check, Trash2, Pencil } from 'lucide-react';
import apiClient from '@/lib/api-client';
import { Button } from '@/components/ui/button';

interface Reminder { id: string; title: string; description?: string; start_at: string; priority: string; status: string; }
const empty = { title: '', description: '', start_at: '', priority: 'NORMAL' };

export default function Reminders() {
  const qc = useQueryClient(); const [form, setForm] = useState<any>(null); const [month, setMonth] = useState(new Date());
  const { data: reminders = [] } = useQuery<Reminder[]>({ queryKey: ['reminders'], queryFn: async () => (await apiClient.get('/reminders')).data.data });
  const save = useMutation({ mutationFn: (data: any) => data.id ? apiClient.patch(`/reminders/${data.id}`, data) : apiClient.post('/reminders', data), onSuccess: () => { qc.invalidateQueries({ queryKey: ['reminders'] }); setForm(null); } });
  const remove = useMutation({ mutationFn: (id: string) => apiClient.delete(`/reminders/${id}`), onSuccess: () => qc.invalidateQueries({ queryKey: ['reminders'] }) });
  const complete = useMutation({ mutationFn: (id: string) => apiClient.patch(`/reminders/${id}/complete`), onSuccess: () => qc.invalidateQueries({ queryKey: ['reminders'] }) });
  const monthKey = `${month.getFullYear()}-${String(month.getMonth() + 1).padStart(2, '0')}`;
  const monthItems = reminders.filter((r) => r.start_at.startsWith(monthKey));
  return <div className="space-y-6 p-6"><div className="flex items-center justify-between"><div><h1 className="text-2xl font-semibold">Hatırlatmalar</h1><p className="text-sm text-muted-foreground">Kişisel görev ve takvim kayıtlarınız</p></div><Button onClick={() => setForm({ ...empty })}><Plus className="mr-2 h-4 w-4" />Yeni Hatırlatma</Button></div><div className="flex items-center gap-3"><Button variant="outline" size="sm" onClick={() => setMonth(new Date(month.getFullYear(), month.getMonth() - 1, 1))}>Önceki</Button><span className="font-medium">{month.toLocaleDateString('tr-TR', { month: 'long', year: 'numeric' })}</span><Button variant="outline" size="sm" onClick={() => setMonth(new Date(month.getFullYear(), month.getMonth() + 1, 1))}>Sonraki</Button></div><div className="rounded-lg border bg-card p-4"><h2 className="mb-3 font-medium">Bu ay</h2>{monthItems.length === 0 ? <p className="text-sm text-muted-foreground">Bu ay için hatırlatma yok.</p> : <div className="space-y-2">{monthItems.map((r) => <div key={r.id} className="flex items-center justify-between rounded border p-3"><div><p className={r.status === 'COMPLETED' ? 'text-sm line-through opacity-50' : 'text-sm font-medium'}>{r.title}</p><p className="text-xs text-muted-foreground">{new Date(r.start_at).toLocaleString('tr-TR')}</p></div><div className="flex gap-1"><Button size="icon" variant="ghost" onClick={() => complete.mutate(r.id)}><Check className="h-4 w-4" /></Button><Button size="icon" variant="ghost" onClick={() => setForm(r)}><Pencil className="h-4 w-4" /></Button><Button size="icon" variant="ghost" onClick={() => remove.mutate(r.id)}><Trash2 className="h-4 w-4" /></Button></div></div>)}</div>}</div>{form && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40"><form className="w-full max-w-md space-y-4 rounded-lg bg-background p-6" onSubmit={(e) => { e.preventDefault(); save.mutate(form); }}><h2 className="text-lg font-semibold">{form.id ? 'Hatırlatmayı düzenle' : 'Yeni hatırlatma'}</h2><input className="w-full rounded border p-2" placeholder="Başlık" required value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} /><textarea className="w-full rounded border p-2" placeholder="Açıklama" value={form.description || ''} onChange={(e) => setForm({ ...form, description: e.target.value })} /><input className="w-full rounded border p-2" type="datetime-local" required value={form.start_at?.slice(0, 16)} onChange={(e) => setForm({ ...form, start_at: new Date(e.target.value).toISOString() })} /><select className="w-full rounded border p-2" value={form.priority} onChange={(e) => setForm({ ...form, priority: e.target.value })}><option value="LOW">Düşük</option><option value="NORMAL">Normal</option><option value="HIGH">Yüksek</option><option value="URGENT">Acil</option></select><div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={() => setForm(null)}>İptal</Button><Button type="submit">Kaydet</Button></div></form></div>}</div>;
}
